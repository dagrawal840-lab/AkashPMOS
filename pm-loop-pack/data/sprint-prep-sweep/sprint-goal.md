# Sprint 42 goal (Jul 20 – Jul 31, 2026)

Ship the self-serve CSV import flow for workspace admins end to end:
upload, column mapping, validation errors surfaced inline, and the
post-import summary email. Stretch: the audit-log entries for imports.

Why now: the top-3 enterprise deals in the pipeline all listed manual
data entry as a blocker in their eval notes, and support logged 41
"how do I bulk add users" tickets last quarter.

Out of scope this sprint: API-based import, scheduled/recurring
imports, anything touching the SSO provisioning path.

Planning meeting: Wed Jul 22, 10:00. Sprint prep sweep runs Tue Jul 21.
